{
  "name": "trax-api-client",
  "version": "1.0.0",
  "description": "A client for use with the Trax application.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "openapi-server-generate-client": "openapi-generator-cli generate -g javascript -i http://localhost:8080/openapi.json --output 'client/release' --skip-validate-spec --additional-properties=usePromises=true,projectVersion=1.0.0,projectName=trax-api-client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.4.26"
  }
}
